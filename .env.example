VITE_DEFRADB_URL=http://localhost:9181
VITE_DEFRADB_TOKEN=
# Set to 1 to build in embedded mode: served by the DefraDB node itself,
# pinned to the serving origin, with connection management hidden.
VITE_EMBEDDED=
